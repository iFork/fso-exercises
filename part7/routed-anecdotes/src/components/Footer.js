import React from 'react';

const Footer = () => {
  const footerStyle = {
    marginTop: '50px',
    // paddingTop: '100px',
    color: 'gray',
  };

  return (
    <div style={footerStyle}>
      Anecdote app
    </div>
  )
};

export default Footer;
